//! Mako binary - runs the main program

// Include the core module for the binary
#[path = "core.rs"]
mod core;

fn main() {
    // The original main function is in core.rs
    // For now, this is a placeholder - the actual main logic is in tests
    println!("Mako OT implementation - use 'cargo test' to run tests");
}
